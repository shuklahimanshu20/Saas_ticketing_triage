# Copy this file to .env and fill in your key, or set the variable in your shell.
# If ANTHROPIC_API_KEY is not set, the app automatically runs in DEMO/MOCK mode
# using a deterministic rule-based classifier instead of a live LLM call.
ANTHROPIC_API_KEY=your_api_key_here
